
AOS.init({
  delay: 200,     // values from 0 - 3000 in 50ms steps
  duration: 1500, // values from 0 - 3000 in 50ms steps 
  once: false,    // should animation happen only once while scrolling down
  mirror: false  // should elements animate out while scrolling past them
})